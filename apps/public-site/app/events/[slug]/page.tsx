import { PageShell } from "@/components/page-shell";

export default function EventDetailPage() {
  return (
    <PageShell
      title="Event Details"
      description="Placeholder shell for an individual event."
    />
  );
}
