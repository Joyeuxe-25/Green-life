import { PageShell } from "@/components/page-shell";

export default function PartnersPage() {
  return (
    <PageShell
      title="Partners"
      description="Placeholder shell for future partner listings, including Restore Local as text until a logo is provided."
    />
  );
}
