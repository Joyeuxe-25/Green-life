import { PageShell } from "@/components/page-shell";

export default function AboutPage() {
  return (
    <PageShell
      title="About"
      description="Placeholder shell for Green Life Rwanda identity, mission, vision, values, and history."
    />
  );
}
